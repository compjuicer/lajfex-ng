{
  "name": "l3",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && esbuild src/api/middleware.js --bundle --platform=node --outfile=dist/server.cjs",
    "start": "node dist/server.cjs",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@jaames/iro": "^5.5.2",
    "express": "^4.19.2",
    "node-lifx-lan": "^0.5.1",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "esbuild": "^0.27.4",
    "prettier": "^3.8.1",
    "vite": "^5.0.0"
  }
}
